optimization
    Find minimum
    ou
    Fit curve: mais interessante. É meio que exatamente o que queremos.

Scipy é uma biblioteca que contem diversas funções uteis para minimização de funções.

Rosenbrock's banana function é um exemplo possível para verificar a performance e robustez de um algoritmo de otimização.

curve.fit model do scipy pega um data set e fita ele numa curva.

Outro método de curve fitting é LSM(least square method), da biblioteca numpy.

O que eu tenho: um exemplo que executa tres funções de minimização. O que eu quero: mais opções? Escrever no relatório?

> Escrever no overleaf e TERMINAR a parte de revisão bibliográfica
> Estudar o pymoo e evolutionary coding
> Rodar um exemplo do ross e entender de onde vou tirar os vetores contendo os pontos dos gráficos
> Trabalhar na issue 532